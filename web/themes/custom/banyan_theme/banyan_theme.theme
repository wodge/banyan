<?php

/**
 * Implements hook_preprocess_HOOK() for page title.
 */
function banyan_theme_preprocess_page_title(&$variables) {
  if (isset($variables['title'])) {
    $variables['title'] = \Drupal\Core\Render\Markup::create(strip_tags(htmlspecialchars_decode((string) $variables['title'])));
  }
}
